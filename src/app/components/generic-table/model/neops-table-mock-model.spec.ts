import { NeopsTableMockModel } from './neops-table-mock-model';

describe('NeopsTableMockModel', () => {
  it('should create an instance', () => {
    expect(new NeopsTableMockModel()).toBeTruthy();
  });
});
